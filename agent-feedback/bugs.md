# Suspected Bugs

Out-of-scope defects noticed while working on something else. Format and rules: [README.md](README.md).

## Resolve `config.root` in the Vite plugin's `config` hook — a relative root yields an empty `@marko/run/router`

`packages/run/src/vite/plugin.ts` › `config` | 2026-08-03 | impact:med | effort:low

The `config` hook sets `root = normalizePath(config.root || process.cwd())`, but Vite runs plugin `config` hooks before it resolves the root, so a relative root (`vite --root app`, `root: "./app"` in a `vite.config.ts`, even `root: "."`) is stored verbatim. Virtual modules are then keyed by `path.posix.join(root, ROUTER_FILENAME)` while `resolveId` hands back `path.resolve(root, ROUTER_FILENAME)`, so `virtualFiles.has(id)` misses in `load` and `@marko/run/router` degrades to an empty module: with `root: "./app"` an SSR build fails with `[MISSING_EXPORT] "fetch" is not exported by "app/__marko-run__router.js"`, and in dev `ssrLoadModule("@marko/run/router")` resolves to `{}` where the same project with an absolute root exports `match`/`invoke`/`fetch`. The same hook also builds `optimizeDeps.entries[0]` from `path.relative(root, routesDir)` with the raw option instead of the `resolvedRoutesDir` computed just above, so whenever Vite's cwd differs from the root the glob picks up a `../` prefix and the startup dep scan skips route templates entirely. Resolve once: `root = normalizePath(path.resolve(config.root || process.cwd()))` and `path.relative(root, resolvedRoutesDir)`. The `marko-run` CLI is immune because `packages/run/src/cli/commands.ts` always passes an absolute `root: cwd`; this hits the direct Vite-plugin setup documented under "Vite Plugin" in `packages/run/README.md` and middleware-mode servers.

## Resolve `build.outDir` with `path.resolve` and empty it the way Vite does

`packages/run/src/vite/plugin.ts` › `config` | 2026-08-03 | impact:med | effort:low

The `config` hook computes `outputDir = path.join(root, config.build?.outDir || "dist")`, but `path.join` mangles an absolute outDir, and the CLI's `-o/--output` flows straight into `build.outDir` (`packages/run/src/cli/commands.ts` › `build`). Building with `-o /abs/out` writes the generated route templates to `<root>/abs/out/.marko-run/index.marko` — a mirrored junk tree inside the project whose empty directories outlive the `closeBundle` cleanup — while the real `/abs/out`, which Vite resolves correctly, is never emptied and keeps stale files from previous builds. The `buildStart` cleanup is also far more destructive than Vite's `prepareOutDir`: gated only on `emptyOutDir ?? true`, it runs `fs.rmSync(outputDir, { recursive: true })`, so `build.outDir: "../out"` deletes that entire directory, `.git` and all, before the client pass fails anyway with `vite:build-html` rejecting the `../out/.marko-run/index.marko.html` entry name — whereas Vite refuses to empty an out-of-root outDir when `emptyOutDir` is unset and its `emptyDir` preserves `.git` and the directory itself. Use `path.resolve(root, config.build?.outDir || "dist")` and mirror Vite's `resolveEmptyOutDir`/`emptyDir` semantics instead of `rmSync`; deferring to Vite's own emptying is not a drop-in, since `prepareOutDir` runs at `renderStart`, after the plugin has already written `<outDir>/.marko-run/*.marko`. Related to the existing entry on moving dev's generated route entries out of the build outDir, which shares this `rmSync` but is about where dev entries live.

## Type the `Schema` failure branch as the raw input shape, not the schema's declared `Input`

`packages/run/src/runtime/types.ts` › `Schema` | 2026-08-03 | impact:med | effort:low

`Schema<I, O>` types the validation-failure element as the schema's declared input `I`, but a value that failed validation is by definition not an `I`: `normalizeValidator` (`packages/run/src/runtime/internal.ts`) returns `[input, result.issues]`, where `input` is the raw record the runtime built — `searchParamsToObject(url.searchParams)` for `search`, `route.params` for `params`, the `JSON.parse` result for `json`, and the parsed form record for `form`. Even a correctly input-typed schema breaks: with `search: v.object({ page: v.pipe(v.string(), v.transform(Number)) })`, omitting `?page` yields `{}` and `?page=1&page=2` yields `{ page: ["1","2"] }`, yet both narrow to `{ page: string }` inside `if (issues)`, so the documented "handle the issues, then re-render" path (`packages/run/cheatsheet.md`) crashes on `value.page.length` with no type error. This also contradicts the cheatsheet's own statement that the failure value is the raw input, and nothing encodes the current typing as deliberate — the `request-body-json`/`request-body-form` fixtures bind the failure value as `_body` and assert only on `issues`. Type the failure element from the raw shape instead — `Record<string, string>` for `params`, `Record<string, string | string[]>` for `search` and `form`, `unknown` for `json` — which `Validation<T>` can thread per key since it already maps by option name, and `Schema` has only one internal reference (`Validated`) to update.

## Pass `out` to the crawler in the static adapter's custom-entry branch so prerendered pages land in the build output

`packages/adapters/static/src/index.ts` › `buildEnd` | 2026-08-03 | impact:med | effort:low

In `buildEnd` the default-entry branch constructs the crawler with `out: path.join(path.dirname(builtEntries[0]), "public")`, but the custom-entry branch — the one that spawns `node <builtEntry>` and crawls it over an undici `Pool` — passes only `origin` and `notFoundPath`. `createCrawler` (`packages/adapters/static/src/crawler.ts`) then falls back to `nodePath.resolve(opts.out || "dist")`, a cwd-relative `dist`, so every prerendered page is written outside the client build: `marko-run build src/index.ts` with the static adapter writes the page to `<cwd>/dist/index.html` while `<outDir>/public` is left empty, and with `--output out` the pages still land in `<cwd>/dist`. Since `startPreview` serves `path.join(dir, "public")` and deploys ship `<outDir>/public`, both preview and the deployed site end up with no pages at all. Pass the same `out` in both branches, and add a `static-adapter-*` fixture that sets a custom `entry` — none currently does, so this branch is untested.

## Strip the query string in the static crawler's `resolvePath` so query links stop emitting unservable `?`-named files

`packages/adapters/static/src/crawler.ts` › `resolvePath` | 2026-08-03 | impact:med | effort:low

`resolvePath` returns `url.pathname + url.search`, and `visit` uses that one string both as the `seen` dedup key and as the on-disk target (`nodePath.join(out, path + ".html")`), so an ordinary `<a href="/posts?page=2">` writes `dist/public/posts?page=2.html`. Static hosts strip the query before resolving a file, so those copies are never served: crawling a stub whose root links `/posts`, `/posts?page=2` and `/search?q=hello%20world` emits `index.html`, `posts.html`, `posts?page=2.html` and `search?q=hello%20world.html`, and serving that directory through the adapter's own sirv config (`startPreview` in `packages/adapters/static/src/index.ts`) answers `/posts?page=2` from `posts.html` and 404s `/search?q=hello%20world`. Worse, a dynamic route reachable only through a query link — root linking `/users/123?tab=posts`, which `buildEnd` does not seed as a param-free path — emits `users/123?tab=posts.html` and no `users/123.html` at all, so the page is simply missing from the build; the query variant also re-renders a route already crawled at its bare path, and `?` is an illegal filename character on Windows, where the write fails outright. Strip `search` in `resolvePath` so query variants collapse onto the already-seen pathname, or keep `search` only in the `seen` key and derive `filePath` from `url.pathname` alone (`resolvePathWithHash` keeping `search` is separate and correct, since it builds the browser-facing meta-refresh URL). No `static-adapter-*` fixture links a query string.

## Emit the prerendered 404 as `404.html` under trailing-slash modes, not `404/index.html`

`packages/adapters/static/src/crawler.ts` › `visit` | 2026-08-03 | impact:med | effort:low

With `trailingSlashes: "RedirectWith"` or `"RewriteWith"`, `buildEnd` in `packages/adapters/static/src/index.ts` passes `notFoundPath: "404/"`, but `visit()` computes `filePath` _before_ `switch (res.status)`, so the `case 404:` trailing-slash trim (`path = path.slice(0, -1)`) no longer affects the output path — after the switch `path` is only read by the `path !== notFoundPath` guard inside `if (redirect)`, which a 404 never reaches. Building an app with `trailingSlashes: "RedirectWith"` and a `+404.marko` emits `dist/public/404/index.html` and no `dist/public/404.html`, while the same app in the default mode emits `dist/public/404.html`. Static hosts (Netlify, GitHub Pages, Cloudflare Pages) key their error page off a root `404.html`, so those deploys serve the host's generic 404 instead of the app's `+404` page. Recompute `filePath` inside the `case 404:` branch after the trim (or move the assignment below the switch while keeping the `case 200:` non-HTML override) so the error page always lands at `<out>/404.html`. Nothing catches it: the `static-adapter-trailing-slash` fixture has no `+404` route, and `startPreview`'s sirv config resolves `/404` to `404/index.html` as readily as to `404.html`.

## Stop silently dropping a grave-escaped literal that starts a flat route pattern

`packages/run/src/vite/routes/parse.ts` › `parseFlatRoute` | 2026-08-03 | impact:med | effort:low

In `parseFlatRoute` the `CharCodes.Directory` branch flushes only `if (current)` and then clears `escaped`, so a grave-escaped literal that opens a pattern is thrown away as soon as the next `.` arrives — the `,` branch and the end-of-pattern flush both honor `escaped`, which is why a lone `` `sitemap.xml` `` directory correctly yields `/sitemap.xml`. Running `buildRoutes` over `` `sitemap.xml`.rss/+handler.ts `` produces `/rss`, and over `` `.well-known`.security`.`txt+handler.ts `` produces `/security.txt`: the route silently mounts at a path the author never wrote, with no error or warning. Escaping a dotted directory and then nesting under it in the same flat name is exactly what the README's "Escaping Control Characters" section leads people to write, and the unescaped spelling is no better — `src/routes/.well-known/security.txt/` quietly becomes `/well-known/security/txt`. Seeding `current` from `basePaths` when `escaped` is non-empty before the Directory flush repairs both shapes without changing any documented behavior; `packages/run/src/vite/__tests__/parse-flat-route.test.ts` has no case for an escape at the start of a pattern.

## Write a 500 status instead of destroying the socket when the middleware has no `next`

`packages/run/src/adapter/middleware.ts` › `createMiddleware` | 2026-08-03 | impact:med | effort:low

When `createMiddleware` catches an error and the caller passed no `next`, the whole recovery is `res.socket?.destroySoon()` plus a `console.error` — no status line is ever written, so the client gets a zero-byte reply. That is the shipped production path: `packages/run/src/adapter/default-entry.mjs`, the default entry for the base adapter and `@marko/run-adapter-node`, invokes `middleware(req, res)` with no third argument. The generated router's `fetch` catches its own errors and returns a bare 500, but everything raised outside it still lands in this branch — `getOrigin` throwing when a request carries no `Host` header and `NODE_ENV` is production, `copyResponseHeaders` throwing `headers is not iterable` when a `+handler` returned a non-`Response`, and a page-render stream that errors before its first chunk — and in all three the response is still unflushed, so proxies and load balancers see a bodiless close, record no status, and may retry a non-idempotent request. In that branch, when `!res.headersSent && !res.destroyed`, drop the headers already copied over, set `res.statusCode = 500` and call `res.end()`, and reserve `destroySoon()` for the already-flushed case where truncating the chunked body is the correct signal.

## Build the `context.body` thenable whenever a `json`/`form` option is configured, not only when the request carries a body

`packages/run/src/runtime/internal.ts` › `createContext` | 2026-08-03 | impact:low | effort:low

`createContext` attaches the body thenable only when the incoming request actually carries one — `route && request.body && (route.options.json || route.options.form)` — so a `POST`/`PUT`/`PATCH` whose `Request.body` is `null` leaves `context.body` as `undefined`. Both the type and the docs promise otherwise: `RouteForFileDef["body"]` in `packages/run/src/runtime/types.ts` resolves to `Promise<...>` with no `| undefined` once a `json`/`form` validator is merged in, and `packages/run/README.md` ("Request Bodies") states that when either option is configured `context.body` is a promise for the parsed body, so the documented `const [data, issues] = await context.body` throws `TypeError: (intermediate value) is not iterable` and 500s. The node adapter masks this because `createMiddleware` always attaches the `IncomingMessage` as the body for those verbs, but a bodyless `context.fetch(path, { method: "POST" })` subrequest, or a hand-built `Request` passed to `Run.fetch`/`Run.invoke` as the README's Express recipe shows, reaches it. Drop the `request.body` condition so a configured validator always yields a thenable — `readBodyWithLimit`'s `throw new Error("Missing request body")` then needs the 400 treatment tracked by the existing "Return 400/413 instead of 500" entry — or widen `RouteForFileDef["body"]` and the README to `| undefined`.

## Relax the `Route` and `GetUpstreamData` type-parameter constraints so `@marko/run`'s own declarations type-check against generated route types

`packages/run/src/runtime/types.ts` › `Route` | 2026-08-03 | impact:low | effort:low

Once an app's generated `.marko-run/routes.d.ts` (`interface App extends $.DefineRoutes<...>`, emitted by `packages/run/src/vite/codegen/index.ts`) is in the TS program, `@marko/run`'s own declarations stop satisfying their type-parameter constraints and `tsc` reports three TS2344 errors inside `packages/run/src/runtime/types.ts` (shipped as `dist/runtime/types.d.ts`) that no app-side change can silence. Two come from `Route`'s `Def extends RouteDef` constraint: the default `RouteDef` has `Options = unknown`, so its `form`/`json` collapse to `never`, while every def `DefineRoute` builds passes `RouteOptionsContainer<Path, Verb>`, leaving `form` a deferred conditional TS cannot reduce to `never` — so `ContextForPath` and `ContextForFile` both violate the constraint on their `Route<...>` instantiations, even for an app that declares no validators. The third comes from `RouteForFileDef` passing its `Path extends keyof AppPaths` into `GetUpstreamData<F, Path extends PathsForFile<F>, Verb>`. Consumers only hit these with `skipLibCheck: false`, but that is a legitimate setting and the errors originate entirely inside the package. Widening the two constraints to `Def extends RouteDef<string, HttpVerb, any, any, any>` and `Path extends PropertyKey` (`GetUpstreamData` already re-guards internally with `Path extends keyof AppPaths ? … : Empty`) clears all three and leaves `ctx.params`, `ctx.method`, `ctx.body` and middleware-derived `ctx.data` resolving identically.

## Resolve `--config` with `path.resolve` so absolute config paths are not appended to the cwd

`packages/run/src/cli/commands.ts` › `getViteConfig` | 2026-08-03 | impact:low | effort:low

`getViteConfig` validates an explicit `--config` with `path.join(dir, configFile)`, but `path.join` does not let an absolute second argument replace the first, so the user's path is appended to `dir` instead. All three CLI actions call `getViteConfig(process.cwd(), opts.config)` (`packages/run/src/cli/index.ts`), so `marko-run build --config "$PWD/vite.config.ts"` aborts with `No config file found at '<cwd>/<cwd>/vite.config.ts'` — a doubled path that reads as if the user's argument were wrong rather than the CLI's join — while the same file passed as a relative path works. Absolute config paths are routine in monorepo and CI scripts, and nothing in `packages/run/src/__tests__/main.test.ts` covers the `--config` branch (it only ever calls `getViteConfig(dir, undefined, undefined, baseViteConfigFile)`). Use `path.resolve(dir, configFile)` for the existence check and return that resolved path, which also makes the branch consistent with default detection, where `findFileWithExt` already returns an absolute path.

## Keep a literal `/404` or `/500` route from colliding with the `+404`/`+500` entry templates

`packages/run/src/vite/routes/builder.ts` › `buildRoutes` | 2026-08-03 | impact:low | effort:low

`buildRoutes` gives the root `+404.marko`/`+500.marko` pages `key: type` and `templateFilePath: <entryFilesDir>/404.marko`/`500.marko`, but never seeds those keys into the `seenKeys` set used to de-dup regular route keys, so a literal `src/routes/404/+page.marko` computes the same key and is handed the identical entry-template path. `renderVirtualFiles` in `packages/run/src/vite/plugin.ts` writes `routes.list` templates first and `routes.special` templates second, so the special page always wins: on a tree with `+404.marko`, `+500.marko`, `404/+page.marko` and `500/+page.marko`, `marko-run dev` answers `GET /404` and `GET /500` with 200 and the special pages' markup instead of the real route pages, and nothing warns. The `route.key === RoutableFileTypes.Error` test in `renderRouteTemplate` (`packages/run/src/vite/codegen/index.ts`) compounds this — any `src/routes/500/+page.marko` is emitted as `<Page error=input.error/>` even when no `+500.marko` exists. Seed `seenKeys` with `RoutableFileTypes.NotFound`/`Error` before `traverse` runs so a colliding route falls back to key `4042`/`5002`, and drive the error-input decision off an explicit flag on `Route` instead of comparing `key`. No fixture under `packages/run/src/vite/__tests__/fixtures` covers a `404`/`500` route directory.

## Reject route files nested under a `$$` catch-all directory instead of letting them hijack the router

`packages/run/src/vite/routes/vdir.ts` › `VDir.addPaths` | 2026-08-03 | impact:low | effort:low

`src/routes/$$rest/foo/+page.marko` is accepted even though `packages/run/README.md` states that catch-all directories cannot nest route files and are not traversed further, and `parseFlatRoute` (`packages/run/src/vite/routes/parse.ts`) throws "nested segments are not allowed after a catch-all parameter" for the equivalent flat form `$$rest.foo+page.marko`. `buildRoutes` parses each directory name in isolation and `VDir.addPaths`/`addDir` never check whether the parent's segment is `$$`, so the route is built with `segments: ["$$", "foo"]`; `createRouteTrie` (`packages/run/src/vite/codegen/index.ts`) then returns at the first `$$`, and the emitted router makes that page the root fall-through — `/` and every path not claimed by a static or dynamic sibling returns `{ path: '/$$rest/foo', params: { rest: pathname.slice(1) } }`, silently discarding the `/foo` the author wrote. If a sibling `src/routes/$$rest/+page.marko` exists, `catchAll ??=` keeps the first insert and `/$$rest/foo` disappears from the router entirely while `renderRouteTypeInfo` still declares it, so `Run.href("/$$rest/foo", { params: { rest: "a" } })` type-checks against an unreachable route and returns `"/undefined"`. Mirror the parse-time error in the builder: throw from `VDir.addDir`/`addPaths` when a segment is added under a dir whose own `segment.type === "$$"` — check the parent segment rather than the source `Path.isCatchall` flag, which belongs to the whole flat expansion and would falsely reject the legal dir name `a.$$rest` — or wire up the `onDir` hook that `packages/run/src/vite/routes/walk.ts` declares and `buildRoutes` never passes.

## Key catch-all dirs by their raw name so sibling `$$a`/`$$b` error instead of silently merging

`packages/run/src/vite/routes/vdir.ts` › `VDir.addDir` | 2026-08-03 | impact:low | effort:low

`VDir.addDir` keys child directories with `segment.type === "$" ? segment.raw : segment.name`, and `segmentEnd` in `packages/run/src/vite/routes/parse.ts` rewrites every catch-all's `name` to the literal `"$$"`, so sibling catch-alls with different param names collapse into one `VDir` while sibling `$` dirs stay distinct. `src/routes/$$a/+middleware.ts` beside `src/routes/$$b/+page.marko` builds a single route with `path.path === "/$$a"` and `params === { a: null }`, silently serving `$$b`'s page under `$$a`'s param name — `params.b` is `undefined` and the generated route types declare `a` — and a `$$b/+handler.ts` or `+meta` is hoisted onto `$$a`'s route the same way; the flat form merges identically since `onFile` goes through the same `addPaths`/`addDir`. Only same-type collisions surface at all, as `addFile`'s `Duplicate file type page added at path /$$`, whereas the `$` analogue is always loud: `buildRoutes`' `uniqueRoutes` check throws `Duplicate routes for path /$`, snapshotted in `packages/run/src/vite/__tests__/fixtures/error-invalid-routes`. The raw-keying for `$` was added specifically to stop this kind of param-name merge and `$$` was left out, so widen the key to `segment.type === "$" || segment.type === "$$" ? segment.raw : segment.name` and let a sibling catch-all collision throw `Duplicate routes for path /$$`.

## Append the dev client-id cookie instead of overwriting `set-cookie` on the response

`packages/run/src/adapter/dev-server.ts` › `getDevGlobal` | 2026-08-03 | impact:low | effort:low

`getDevGlobal`'s `onClient` writes its correlation cookie with `res.setHeader("set-cookie", ...)`, and `createMiddleware` (`packages/run/src/adapter/middleware.ts`) calls it as the first statement of every `text/html` request whenever `NODE_ENV` is unset or `development` and `globalThis.__marko_run_dev__` exists. Because `setHeader` replaces the whole header, any `Set-Cookie` an upstream Connect/Express middleware already queued on `res` is silently dropped: with `@marko/run-adapter-node`'s `routerMiddleware` mounted after a middleware that does `res.cookie(...)` then `next()` — the layout the node adapter README documents, and the one `ensure-runtime.ts` installs the dev global for — the upstream cookie never reaches the browser in dev, while the same server works in production where `onClient` is skipped. The adapter already handles this correctly in the other direction: `copyResponseHeaders` special-cases `set-cookie` to `appendHeader`, pinned by `packages/run/src/__tests__/adapter-middleware.test.ts` › "should preserve existing cookies set". Switch `onClient` to `res.appendHeader("set-cookie", ...)`, which sets the header when none exists and leaves upstream cookies intact.

## Send the dev worker's entry-load error to `ws.send` as an object so Vite delivers it as an `error` payload

`packages/run/src/adapter/load-dev-worker.mjs` › `start` | 2026-08-03 | impact:low | effort:low

In `start`, the `loader.ssrLoadModule(entry)` rejection handler broadcasts the failure with `devServer.ws.send(JSON.stringify({ type: "error", err }))`, but Vite's `WebSocketServer.send` treats a string first argument as a custom event name, so connected clients receive `{"type":"custom","event":"{\"type\":\"error\",…}"}` — an event nobody listens for, so no error overlay appears and the payload also misses Vite's replay buffer, which only holds `error`/`full-reload` payloads for the next client to connect. Pass the object directly, exactly as `shutdown()` a few lines below already does with `{ type: "full-reload" }`. The superficially identical `devWebSocket?.send(JSON.stringify(...))` in `packages/run/src/adapter/middleware.ts` must stay as-is: that socket comes from `devServer.ws.on("connection", …)` in `packages/run/src/adapter/dev-server.ts`, which Vite forwards to the raw `ws` server, where a string is the frame body. Scope is small — dev-only, custom-entry-only, and the adjacent `console.error(err)` still prints the failure in the terminal.

## Handle explorer build failures instead of crashing the explorer worker on the first request

`packages/explorer/lib/server.js` › `build` | 2026-08-03 | impact:low | effort:low

`build()` calls `reject(error)` without returning, so a failed `marko-run build` falls straight through to `await import(<appDir>/index.mjs)` — a file that does not exist precisely because the build failed — and the async `exec` callback rejects a second time with nothing attached to catch it. The lazy `middleware = (req, res) => { build().then(() => middleware(req, res)) }` also attaches no `catch`, so the first rejection is unhandled as well. Under Node's default `--unhandled-rejections=throw` either one kills the forked explorer worker (`spawnServerWorker` via `startExplorer` in `packages/run/src/adapter/index.ts`): with `marko-run` missing from the exec'd shell's `PATH`, or the install dir read-only, the very first request gets an empty reply, the explorer URL printed by `logInfoBox` is dead for the rest of the session, and all the user sees is a raw V8 stack in the dev output. Return after `reject`, `catch` in the middleware and reply 500 with the captured build output, and reset `buildPromise` on failure so the next request retries instead of replaying the cached rejection.

## Write the crawled 404 page only for the 404 path itself, not for every path that answers 404

`packages/adapters/static/src/crawler.ts` › `visit` | 2026-08-04 | impact:med | effort:low

With a `+404` route the adapter passes `notFoundPath`, and `visit`'s `case 404:` then breaks out of the switch for _any_ path that answered 404, so the 404 page's own markup is written to `<out>/<path>.html` for each one. A link to a page that no longer exists therefore ships as a real file: a fixture whose page links `/gone` emits `dist/public/gone.html` containing the `+404.marko` markup, and driving the adapter's own `startPreview` over that build answers `GET /gone` with `200` and the not-found markup (verified; static hosts resolve `/gone` to that file the same way). That is a soft 404 — clients, caches and search crawlers see a success at a URL that does not exist, and the host never falls back to the root `404.html` it would otherwise serve with a real 404 status. Write the page only when `path === notFoundPath` (the seeded 404 route) and treat every other 404 the way the pre-`notFoundPath` branch already does — abort and write nothing — which `crawl`'s summary now reports under "Paths that answered 404" either way.

## Answer an unknown path in static preview with the 404 body, not a redirect to `/404`

`packages/adapters/static/src/index.ts` › `startPreview` | 2026-08-04 | impact:low | effort:low

`startPreview`'s sirv fall-through answers any path that matched no file with `res.writeHead(302, undefined, { location: "/404" })`, so preview reports a not-found URL as a redirect to a page that answers 404, while every static host the adapter targets answers 404 at the requested URL itself. A test or crawler asserting that an unknown URL 404s sees the 302 first, and preview is the one place that shape can be caught before deploy. Serve `404.html`'s body from the fall-through with an explicit `res.writeHead(404)` rather than redirecting to a path that exists in the build output. The `/404` page's own status is no longer part of this -- `forceStatus` pins it around sirv, which is what sirv's `setHeaders` could not do.

## Routes 404 after Vite's in-place restart when the config graph changes

`packages/run/src/vite/plugin.ts` › `virtualFiles` | 2026-08-10 | impact:med | effort:med

If `vite.config.ts` imports project source (e.g. to start a sidecar server from `configureServer`), editing any file in that import graph makes Vite restart in place — and after that restart every route answers 404 until the whole `marko-run dev` process is killed and restarted. Observed repeatedly in a real app; the workaround was spawning the sidecar as a child process (`spawn(process.execPath, [entry])`) so the config graph never touches server code, plus explicit `.ts` extensions to keep imports out of the graph. Suspect the `virtualFiles` map / route-walker state is not rebuilt on Vite's in-place restart path the way it is on first boot. Re-verify: a `marko-run dev` app whose `vite.config.ts` imports any file under `src/`; touch that file, wait for Vite's restart log, then curl any route — 404 until process restart.

## Dev server drops WebSocket upgrades: `server.proxy` ws entries never connect, unmatched upgrades answer 404

`packages/run/src/adapter/dev-server.ts` › `createViteDevServer` | 2026-08-10 | impact:high | effort:med

Dev runs Vite in middleware mode behind marko-run's own listener (`devServer.middlewares.listen` in `startDev`, `packages/run/src/adapter/index.ts`), so `server.httpServer` is `null` inside every Vite plugin and no upgrade path exists. Three verified consequences for an app that serves its own WebSocket: (1) a user config `server.proxy: { "/term": { target: "ws://localhost:7172", ws: true } }` completes the client handshake but never establishes the upstream — the socket opens, receives zero frames, and dies with 1006, silently; (2) without the proxy entry, an upgrade request for an app path is processed as a plain request and the router writes an HTTP 404 to the wire (a `ws` client reports "Unexpected server response: 404"); (3) a plugin cannot work around it, since `configureServer` never sees the real http server. Direction: hand the listener to Vite (`server.hmr.server`) and honor `server.proxy` ws entries against it — the same listener the existing HMR-port entry needs. Until then the only workaround is fronting the dev server with an external path-routing proxy. Re-verify: `marko-run dev` app with that proxy config, `new WebSocket("ws://localhost:<port>/term/x")` — open then 1006 with no upstream traffic; remove the proxy entry and the handshake gets 404.

## Explorer entry column shows "Handler" for verbs the handler does not export

`packages/explorer/src/routes/+page.marko` › route table `col-entry` | 2026-08-14 | impact:low | effort:low

The entry cell prints "Handler" whenever `route.handler` exists, without checking that the handler actually exports the row's verb. A route with a page plus a POST-only handler renders its GET (and now QUERY) rows as "Handler → Page", but those requests go straight to the page render — the handler never runs. Gate the "Handler" label on `route.handler.verbs.includes(verb)` (treating the no-verb/"all" row as handler-backed) so the per-verb entry matches what the generated router does.

## Any middle segment makes a file a route, so `+handler.test.ts` registers as a second handler

`packages/run/src/vite/routes/builder.ts` › `RoutableFileRegex` | 2026-08-14 | impact:med | effort:low

`nonMarkoFiles` (and `markoFiles`) allow an arbitrary middle segment — `(+middleware|+handler|+meta)\.(?:.*\.)?(.+)` — and nothing ever reads that group (`matchRoutableFile` and `onFile` only use the type and the trailing extension). A colocated `+handler.test.ts` next to `+handler.ts` therefore builds a second `RoutableFile` of type `handler` for the same directory, and `VDir.addFile` takes both. The same holds for `+page.test.marko`. Either drop the optional middle segment, or ignore known test/spec segments there. Re-verify: add `+handler.test.ts` beside `+handler.ts` in a route directory and inspect `buildRoutes` output.

## Make `Run.href`'s `search` keys optional; the documented single-key call does not compile

`packages/run/src/runtime/types.ts` › `HrefBaseOptions` | 2026-08-20 | impact:high | effort:low

`HrefBaseOptions.search` is the homomorphic mapped type `[K in keyof Valid<GetRawSearchValidator<Path>>]: string | number | null | undefined`, which preserves the validator's required modifiers, so every key the route's `search` validator returns becomes mandatory at every call site. On a route whose validator returns `{q, from, to, sort, dir, page}`, the exact shape the README's "Typed URLs" section and `website/docs/marko-run/runtime.md` show — `Run.href("/tx", { search: { q: "figma" } })` — fails `mtc` with `TS2739 Type '{ q: string; }' is missing the following properties … from, to, sort, dir, page`. The modifier buys nothing, because `undefined` is already an accepted value and `href` omits those entries from the output, so the only effect is that every link must spell out five `: undefined` keys or funnel through a hand-written `partial → full` helper, which also forfeits the build-time `Run.href` folding the client build does on direct calls. Making the mapped type optional (`]?:`) accepts the documented call, still rejects an unknown key with `TS2353`, and leaves an app that already spells every key out type-checking unchanged.

## Type `Run.href`'s `search` as a string record when the route declares no `search` validator; today it is `{}`

`packages/run/src/runtime/types.ts` › `HrefBaseOptions` | 2026-08-20 | impact:low | effort:low

`HrefBaseOptions.search` maps over `keyof Valid<GetRawSearchValidator<Path>>`, and `GetRawSearchValidator` resolves to `never` for any route without a `search` option, so the whole option collapses: `HrefOptions<"/">["search"]` probes as exactly `{}`. Everything then type-checks — `Run.href("/", { search: 5 })`, `Run.href("/", { search: { bogusKey: "x" } })` and `Run.href("/", { search: { bogusKey: { nested: true } } })` all pass `mtc` clean, and the last serializes `?bogusKey=%5Bobject%20Object%5D` — while the path argument on the same line is strictly checked against the route union and the same misspelled key _is_ caught (TS2353) once any validator is declared. Since most routes never declare one, "type checking of the path, params, search, and hash" (`packages/run/README.md`, Typed URLs) holds for a minority of call sites, and the failure is silent in the direction that matters: a typo'd or wrongly-typed query key ships a link nothing rejects. Fall back to `Record<string, string | number | null | undefined>` instead of the empty mapped type when the route has no validator — that keeps arbitrary keys legal (a route may still read `$global.url.searchParams`, and analytics params are legitimate) while rejecting the non-serializable values, and it mirrors how `params` falls back to `PathParams<Path>` rather than to nothing. The same mapped type is wrong in the other direction too, making every key a function validator returns mandatory at every call site; both are one edit to `HrefBaseOptions` and both want a case in `packages/run/src/__tests__/typecheck.test.ts`, which has no `href` coverage today.

## Accept an `interface` in `next(data)` instead of requiring an index signature

`packages/run/src/runtime/types.ts` › `NextFunction` | 2026-08-20 | impact:med | effort:low

`NextFunction`'s data overload is `<Data extends Record<string, unknown>>(data: Data)`, and an `interface` has no implicit index signature, so a payload declared the way most style guides recommend is rejected: declaring `interface PageData { … }` instead of `type PageData = { … }` in a `+handler.ts` turns `next(page({…}))` into `TS2345 Argument of type 'PageData' is not assignable to parameter of type 'Record<string, unknown>'. Index signature for type 'string' is missing in type 'PageData'`. The damage is mostly downstream: `Route["data"]` and `RouteForFileDef["data"]` extract with `infer T extends Record<string, unknown>`, which the interface also fails, so `data` falls back to `Record<string, unknown>` and the template that reads it fills with `TS18046 'employees' is of type 'unknown'` and `TS2339 Property 'id' does not exist on type 'never'`. On one 4-route app that was 2 errors at the `next()` calls and 12 more in `+page.marko` blaming the markup; flipping the one `interface` back to `type` cleared all 14. Widening the four `Record<string, unknown>` constraints in `NextFunction`, `Route["data"]`, `RouteForFileDef["data"]` and `HandlerTypes["data"]` to `object` type-checks that app clean with the `interface` spelling and leaves the `type` spelling resolving identically, so the fallback `Record<string, unknown>` only has to stay in the _false_ branches.

## Count a route's CSS in the build table's SIZE/GZIP column, or say the column measures JS only

`packages/run/src/vite/utils/log.ts` › `computeChunkSize` | 2026-08-20 | impact:low | effort:low

`computeRouteSize` looks up the client entry chunk whose `facadeModuleId` is `<templateFilePath>.html` and `computeChunkSize` sums `chunk.code` plus, recursively, `chunk.imports` — JS only. `chunk.viteMetadata.importedCss` is never consulted, so the number understates any route that ships styles and is simply wrong for a route that ships nothing else: in a two-route app whose `/tx` page emits `tx-*.js` (13,674 B), its own `tx-*.css` (2,939 B) and a shared `_layout-*.css` (539 B), the table prints `13.7/6.3 kB` — exactly the JS chunk — while a page with no client component that still links 4,253 B + 470 B of stylesheet prints `0.0 kB`, because with no JS there is no entry chunk to find and `computeRouteSize` returns `undefined`. Read as printed, `0.0 kB` says "this route ships nothing", and the table is the main at-a-glance view of a route's cost. Add the entry's `viteMetadata.importedCss` (and `importedAssets`) into `computeChunkSize`, recursing through imported chunks the same way it already does for JS, and decide what a CSS-only route should report given it has no chunk to hang the bytes on — or rename the column so it reads as the route's JS entry size. Worth pinning in the same pass: the ~900 B of inline resume runtime every streamed page carries comes from marko's HTML output rather than the bundle, so it is out of this column's reach and should not be conflated with the CSS gap.

## Answer a wrong-verb request to an existing page path with 405 and an `Allow` header, not the 404 page

`packages/run/src/vite/codegen/index.ts` › `renderRouter` | 2026-08-20 | impact:med | effort:med

Against a production build of a route with a `+page.marko` and no `+handler`, `POST /venues/new` answers `404` and renders `+404.marko`; `PUT`, `DELETE` and `OPTIONS` do the same, while `HEAD` correctly 200s off the GET matcher. `renderRouter` emits one `case '<VERB>':` block per supported verb inside `match_internal` and returns `null` for every other method, so `invoke` falls through to the same generic 404 it uses for a path that does not exist — even though the path is in the build's own route table and only the verb is unsupported. Posting a form to a page that has no POST handler is a routine mistake, and a 404 sends the author hunting for a routing bug that is not there; `405`, `Method Not Allowed` and `Allow` have no occurrences anywhere under `packages/run`. The route trie already knows the full verb set for each path, so `match_internal` can distinguish "no such path" from "path exists, wrong verb" and `invoke` can answer `405` with an `Allow` header derived from the same table.

## Make a catch-all route match its own parent path, or stop `Run.href` from emitting that path

`packages/run/src/vite/codegen/index.ts` › `writeRouterVerb` | 2026-08-20 | impact:med | effort:low

With `src/routes/archive/$$rest/+page.marko` as the only route under `/archive`, `GET /archive/a/b` answers 200 but `GET /archive` and `GET /archive/` both 404 — and `/archive/` does not even get the trailing-slash redirect every matched route gets. The generated matcher makes it structurally unreachable: the catch-all is emitted as `else if (pathname.slice(1, i1 - 1) === "archive") return …`, on the branch taken only when `i1` (the index after the next `/`) is neither 0 nor the end of the string, so a zero-segment remainder never reaches it. The framework contradicts itself here: `href("/archive/$$rest", { params: { rest: [] } })` returns `"/archive/"`, a URL its own router answers 404, while `packages/run/README.md` says a catch-all matches "to the end of the path" and is how you build "404 Not Found routes at any level, including the root". Either match with an empty remainder (`rest === ""`) or reject an empty catch-all param in `href` and say so in the README; add match fixtures covering `/archive`, `/archive/` and `/archive/a/b` beside the existing `dynamic-rest` fixture, which only exercises a populated remainder.

## Default the client build's `sourcemap` off (or to `hidden`); today every deploy publishes original template source

`packages/run/src/vite/plugin.ts` › `config` | 2026-08-20 | impact:med | effort:low

The `config` hook sets `pluginConfig.build.sourcemap ??= config.build?.sourcemap ?? (isBuild && !isSSRBuild)`, so unless an app names `build.sourcemap` itself the client pass is built with full external source maps — Vite's own default is `false`. A stock `marko-run build` of an app with one interactive page writes `dist/public/assets/heavy-DOUhrDvS.js` at 2,577 bytes next to `heavy-DOUhrDvS.js.map` at 73,457 bytes, whose `sources` are `../../../src/routes/heavy/+page.marko` and `marko/dist/dom-*.mjs` and whose `sourcesContent[1]` is the verbatim template text; the bundle ends in `//# sourceMappingURL=heavy-DOUhrDvS.js.map`, so browsers and crawlers fetch it. That is a 28x asset-size multiplier and the app's unminified original source on the public origin, from a build the author never configured, and no `@marko/run` doc mentions source maps at all. Default the client build to `false` (or `"hidden"`, keeping the map for upload without the `sourceMappingURL` comment) and let apps opt in, since the SSR pass — where a readable stack is actually worth something — is already excluded by the `!isSSRBuild` half of the same expression.

## Stop logging `200` for a dev request that never produced a response

`packages/run/src/adapter/logger.ts` › `logResponse` | 2026-08-20 | impact:low | effort:low

`done("close")` reads `res.statusCode` and prints it, but when the socket closes before anything is written that value is still Node's unset default, so a request that produced no response at all is logged as a success status. A handler returning a never-settling promise, hit with `curl --max-time 4` (client sees `000`), logs `━━x GET /hang 200 4002ms -`; the same shape appears for every request against a wedged dev server — `━━x GET /r5 200 20.0s -` for URLs `curl` reported as `000`. The failure arrow `x` is the only signal that anything went wrong, and it is easy to read past next to a green-looking `200` and a `-` byte count, which is why a hung dev server reads as a working one in `dev.log`. Gate the status column on `res.headersSent` (or on the `close`-without-`finish` path `done` already distinguishes) and print a distinct terminal state instead. Same file as the filed cleanup entry on the leaking in-flight `⁺` slot, so both can be covered by one test over the logger middleware.

## Recover the dev server after a bulk route-file rewrite instead of wedging every later request forever

`packages/run/src/vite/plugin.ts` › `configureServer` | 2026-08-20 | impact:high | effort:med

Rewriting the contents of every file under `src/routes` in one fast pass — what `prettier --write` with real changes, a codemod, a `git checkout` or a format-all-on-save does — permanently wedges `marko-run dev`. On the `app` starter grown to 18 route directories (50 files under `src/routes`), `GET /r5` answers 200 in 73 ms; after a single pass appending a newline to every `.marko`/`.ts` under `src/routes`, the next requests return `000` at a 20 s client timeout twice and then 500, and the dev log carries `transport invoke timed out after 60000ms (data: {"type":"custom","event":"vite:invoke","data":{"name":"fetchModule",…,"data":["@marko/run/router",null,{"cached":false,"startOffset":3}]}})` thrown from vite's `reviveInvokeError`. It never recovers: another 35 s of waiting, a single-file `touch` and a further request all return `000`, while the process sits at 2.6% CPU — a promise that never settles, not a spin — so only SIGTERM and a restart get the app back. It reproduced 2 of 2 times with one fast pass and 0 of 1 with a slower per-file pass (one `sh -c` per file), so it is a race in the burst: the watcher's `all` listener calls `invalidateVirtualFiles()` (clearing `renderVirtualFilesResult` mid-flight) while `writeEntryTemplate` re-emits `watcher.emit("change", …)` from inside the pass it just invalidated, and the `load` of `@marko/run/router` that every request awaits via `renderVirtualFiles` never resolves. The existing dx entry on add/remove churn covers reload volume, not a permanent wedge; add a test that rewrites every route file under a live dev server and asserts the following request answers.

## Turn a multipart limit breach into `issues` instead of a bodiless 413 that also discards the fields parsed before it

`packages/run/src/runtime/internal.ts` › `readBody` | 2026-08-20 | impact:med | effort:med

`readBody` catches the parser's limit errors and rethrows them stamped with a status (`clientError("Request body too large", 413, error)` keeps the original error object), so `await ctx.body` rejects and an unhandled rejection becomes `HTTP/1.1 413 Payload Too Large` with `content-length: 0` — a literally blank page for a no-JS form post. Handled, it is barely better: `Run.POST({ form: { maxFiles: 2, maxFileBytes: 1000, validator: (v) => v } }, ...)` posted `-F caption=hello -F receipts=@4k.txt` yields `MaxFileSizeExceededError` with message `File size exceeds maximum allowed size of 1000 bytes` and `Object.keys(err)` of `["name"]`, so per-file messages are impossible, and the `caption` text field that parsed before the offending part is thrown away with the body. Neither error class is reachable from `@marko/run` — they come from the transitive `@remix-run/form-data-parser` and the package `exports` map has no error surface — so `err.constructor.name` is the only discriminator an app has. The README frames `form` options as validation that re-renders the page with `issues` rather than a bare 400; a limit breach is exactly the case that cannot do that today, so surface it as an `issues` entry naming the field and file with the already-parsed fields in the value half of the tuple, and at minimum re-export the four error classes. The "Build the `context.body` thenable whenever a `json`/`form` option is configured" entry defers its 400 handling to a "Return 400/413 instead of 500" entry that is no longer in this backlog; this is where that work now lives.

## Fail when more than one `@marko/run-adapter-*` is installed instead of silently picking the first

`packages/run/src/vite/plugin.ts` › `resolveAdapter` | 2026-08-20 | impact:high | effort:med

`resolveAdapter` concatenates `dependencies` and `devDependencies` and returns the first name matching `@marko/run-adapter*` or `marko-run-adapter`, announcing its choice only through `debug()`. With `@marko/run-adapter-netlify`, `-node` and `-static` all installed and no explicit `adapter` option, `marko-run build` picks netlify on package.json key order and writes a Netlify Functions module (`export { config, fetch as default }`) to `dist/index.mjs`; the build prints its usual route table, exits 0, names no adapter, and `node dist/index.mjs` then exits 0 immediately with no output and no listener — a successful-looking build and start that serves nothing. Having two or three adapters installed is normal while evaluating deploy targets, and nobody expects dependency ordering to select one. Throw when auto-discovery finds more than one adapter package and no `adapter` option was given, naming the packages it found and the `marko({ adapter })` option, and print the resolved adapter in the build output instead of only behind `DEBUG`.

## Skip the empty placeholder part a browser sends for an untouched file input before calling `onFile`

`packages/run/src/runtime/internal.ts` › `readBody` | 2026-08-20 | impact:low | effort:low

Every browser submits an untouched `<input type="file">` as a part with `filename=""` and a zero-byte body, and `readBody` forwards `onFile` straight into `parseFormData`, so the hook fires for it. Feeding `@remix-run/form-data-parser` the exact bytes a browser sends produces `onFile("receipts") name="" size=0` and no form entry, so every app has to hand-write `if (!file.name && file.size === 0) return;` or report "unnamed: empty file" at a user who did nothing wrong. Wrap the callback passed at the `parseFormData` call site so a part with an empty name and no bytes never reaches the app's `onFile`. Note that the parser increments its own `fileCount` against `maxFiles` before invoking the handler, so a form with several optional file inputs spends that budget on placeholders either way; if that matters the limit has to be enforced on run's side of the callback.

## Narrow `form.onFile`'s return type; an object returned from it lands in the parsed field as `"[object Object]"`

`packages/run/src/runtime/types.ts` › `FormBodyValidatorOptions` | 2026-08-20 | impact:med | effort:low

`onFile?(ctx: Ctx, file: Multipart): any` widens a return type that `@remix-run/form-data-parser` declares precisely — its `FileUploadHandler` returns `void | null | string | Blob | Promise<void | null | string | Blob>` — and `parseFormData` feeds whatever comes back to `formData.append(fieldName, value)`, which USVString-coerces anything that is not a `Blob`. So the natural thing to return after saving an upload, the metadata record `{ id, path }`, silently becomes the string `"[object Object]"`: `Run.POST({ form: { onFile: () => ({ id: 1, path: "/tmp/x" }), validator: (v) => v } }, ...)` posted with two files answers `{"receipts":{"type":"object","value":"[object Object],[object Object]"}}`. Nothing catches it — the option is typed `any`, so `mtc` is silent, and the app has to smuggle metadata through a JSON string or a side channel keyed on `ctx`. Reuse the parser's own return type on the `onFile` signature so the mistake is a compile error, and say in the README's `form` bullet that the returned value is what is stored in the field.

## Stop letting `new URL` collapse percent-encoded dot segments: `/a/%2e%2e/b` silently serves route `/b`

`packages/run/src/adapter/middleware.ts` › `createMiddleware` | 2026-08-20 | impact:med | effort:low

`%2e` is an escaped literal `.`, so `/service/%2e%2e/raw` names a segment called `..`, but `createMiddleware` passes the raw target to `new URL(req.url!, origin)` and the WHATWG parser treats `%2e%2e` as a double-dot path segment and resolves it away. On a default node build the request returns `200` with the `/raw` page and `$global.url` serialized as `new URL("http://localhost:PORT/raw")` — no redirect, no 404, and the browser keeps the disguised URL. This is the classic proxy-vs-origin normalization mismatch: a gateway that authorizes `/service/*` by prefix passes `/service/%2e%2e/admin` through to a route it was meant to protect, and any `+middleware.ts` on the abandoned prefix never runs because matching happens on the collapsed path. Decide it explicitly at the same place the `//` case is handled: either reject a target containing a `%2e`-spelled dot segment, or canonicalize with a 308 before matching so the served path and the requested path agree; a parse/match fixture over `%2e%2e`, `..` and `.` segments would pin whichever is chosen.

## Reject a request target that starts with `//`; the WHATWG parser reads it as protocol-relative and hands the attacker `ctx.url.origin`

`packages/run/src/adapter/middleware.ts` › `createMiddleware` | 2026-08-20 | impact:high | effort:low

`createMiddleware` builds the request URL as `new URL(req.url!, origin || getOrigin(req, trustProxy))`, and a request target beginning with `//` is protocol-relative, so the base is discarded entirely: `new URL("//evil.example.com/raw/", "http://localhost:3000")` is `http://evil.example.com/raw/`. Against a default `marko-run build` served by `@marko/run-adapter-node`, `curl --path-as-is 'http://localhost:PORT//evil.example.com/raw/'` answers `302 Found` with `location: http://evil.example.com/raw` — the framework's own `RedirectWithout` trailing-slash canonicalization (`packages/run/src/vite/codegen/index.ts` › `renderTrailingSlashPolicy`) turned into an open redirect on every app that has not opted out. Without the trailing slash it is worse: the same path returns `200` with the app's page and the serialized `$global.url` reads `new URL("http://evil.example.com/raw")`, so every `ctx.url`-derived absolute link, canonical tag and sitemap entry in the response is rooted at the attacker's host, and `ctx.redirect("/x")` resolves against it too. Normalize before constructing the URL — treat a target whose first two characters are `/` `/` as a path, not an authority (`req.url.replace(/^\/+/, "/")` or an explicit 400) — and pin it with a fixture asserting `ctx.url.origin` equals the `Host`-derived origin for a `//`-prefixed target.

## Settle out-of-order streaming boundaries before writing a prerendered page, or report them

`packages/adapters/static/src/crawler.ts` › `createCrawler` | 2026-08-20 | impact:high | effort:med

`visit` pipes the SSR response body straight into the page's write stream, so a page whose content sits behind a `<try>`/`<@placeholder>` boundary is frozen mid-stream on disk. A route rendering `<try><await|msg|=…><p>REAL CONTENT</p></await><@placeholder><div role=status aria-busy>LOADING SKELETON</div></@placeholder></try>` builds to `dist/public/stream.html` with the skeleton in document flow inside `<main>` and the real content parked after `</main>` as `<t hidden M_=b><p>REAL CONTENT</p></t><script>M._.w()</script>` — while the summary reads `Crawled 4, success 4, failed 0, redirect 0, not found 0`. A prerendered file is exactly the artifact that no-JS clients, crawlers and CDNs consume, and nothing is ever going to stream into it, so for them the page shows a loading skeleton permanently; the `success` count says the opposite. Either settle the response before writing (the crawler already has the whole body in hand and there is no client waiting), or scan the emitted HTML for unresolved `<t hidden M_=` markers and report each affected path — the same place the crawler already classifies 404s and redirects. The static-adapter docs never mention the interaction with `<try>`/`<@placeholder>` at all.

## Make the trailing-slash redirect method-preserving and keep it inside an embedded mount's prefix

`packages/run/src/vite/codegen/index.ts` › `renderTrailingSlashPolicy` | 2026-08-20 | impact:med | effort:low

Under the default `trailingSlashes: "RedirectWithout"`, `renderTrailingSlashPolicy` emits `return Response.redirect(url)`, whose default status is 302 — a status clients are free to (and do) rewrite. Against a route with a POST handler, `curl -X POST -d name=x http://localhost:41973/venues/` answers `302` with `location: http://localhost:41973/venues`, and following that redirect reaches the handler with an empty body: it echoes `posted ` where a direct `POST /venues` echoes `posted name=x`. So a form posted to a URL that merely carries a trailing slash is silently emptied, with nothing logged; 308 preserves method and body and is the right status for a canonicalization the router performs unconditionally. The second half is the absolute `location`: it is rebuilt from `request.url`, which a Connect mount has already stripped of its prefix, so `app.use("/status", routerMiddleware())` answers `POST /status/venues/` with `location: http://localhost:41972/venues` — outside the mount, where the host app 404s it. Emit 308 (307 for `RedirectWith`) and build the `location` as a reference the mount cannot invalidate, with a fixture per `trailingSlashes` mode asserting both status and header; note also that `packages/run/README.md` has zero hits for `trailingSlashes`, so the default 302 is invisible to anyone reading the package's own docs.

## Keep a running `marko-run dev` alive when `marko-run build` empties `dist/`

`packages/run/src/vite/plugin.ts` › `buildStart` | 2026-08-20 | impact:high | effort:med

Dev writes its generated route templates to `entryFilesDir = path.join(outputDir, ".marko-run")` (`plugin.ts:488`), i.e. inside the build's output directory, and `buildStart` opens the SSR pass with `fs.rmSync(outputDir, { recursive: true })`. Running a build in a second terminal while a dev server is up therefore deletes the files the dev server is serving from: `curl localhost:4000/` returns 200 before `npx marko-run build` and 500 after it, with `ENOENT: no such file or directory, scandir '<root>/dist/.marko-run'` raised from the Marko translator's `getStyleFile` while compiling `<root>/dist/.marko-run/index.server-entry.marko`. It never recovers — every later request 500s, and neither waiting nor editing a source file restores it; only restarting dev does. Running a build against a checkout you are also developing in (a CI-shaped smoke check, a colleague's script, a `preview` in another pane) is ordinary, and nothing warns that it will take the dev server down. Either put dev's generated entries somewhere the build does not empty, or have `buildStart` skip/limit the removal when a dev server owns that directory; the destructive `rmSync` is the same one the existing `build.outDir` entry proposes replacing with Vite's `emptyDir` semantics.

## Declare the `$global` fields Marko owns on `Context`: `ctx.cspNonce = …` is a type error in a middleware

`packages/run/src/runtime/types.ts` › `Context` | 2026-08-20 | impact:med | effort:low

`createContext`'s object is handed to Marko verbatim as the page's `$global` — `template.render({ ...input, $global: context as unknown as Marko.Global })` in `packages/run/src/runtime/internal.ts` › `createContext` — and `Context`'s own docstring says it is "`ctx` in middleware and handlers and `$global` in templates". But `Marko.Global` declares `cspNonce`, `signal`, `renderId`, `runtimeId` and `serializedGlobals` over an `[x: PropertyKey]: unknown` index signature, while run's `Context` declares only `serializedGlobals` and has no index signature. So the single assignment a CSP implementation must make, `ctx.cspNonce = crypto.randomUUID()` in a `+middleware.ts`, is `error TS2339 Property 'cspNonce' does not exist on type 'Context<RouteForFileDef<M1, "/", "GET", Empty>> | …'` under `mtc`, and the only escape is a cast (`(ctx as unknown as { cspNonce: string }).cspNonce = nonce` takes the same app to zero errors) in the one file whose job is security-critical. The mechanism works at runtime and is the documented one — markojs.com's template reference documents `$global.cspNonce` as how Marko nonces the `<script>`/`<style>` elements it renders — so only the type disagrees. Declare the writable Marko-owned globals on `Context`, and add them to the `### Context` property list in `packages/run/README.md`, which omits `cspNonce` and `serializedGlobals` alike.

## Scope a template compile error to the route that owns it; today one bad `+page.marko` 500s every route

`packages/run/src/vite/codegen/index.ts` › `renderRouter` | 2026-08-21 | impact:high | effort:med

`renderRouter` emits one module that statically imports every route's virtual entry, and `createDevServer` (`packages/run/src/adapter/dev-server.ts`) awaits `ssrLoadModule("@marko/run/router")` before _every_ request, so a compile error in any one template fails the module load for all of them. On the `app` starter with a valid `src/routes/probe1/+page.marko`, breaking the unrelated root `src/routes/+page.marko` (`<h1>BROKEN<` against `</section>`) turns `GET /probe1` from 200 into a 500 whose body quotes `src/routes/+page.marko:3:1` and that file's code frame — an error page naming a file the developer may not have open, on a route that compiles fine. Nothing records it either: `createErrorMiddleware` only calls `config.logger.error` when `!error.id`, and a Marko compile error carries an `id`, so the dev log shows an unbroken run of `GET /probe1 500 0ms 1.47kb` lines with no compile failure, no file-change event and no recompile. Combined with the stale-save wedge in the neighbouring entry, one mistyped character makes the whole app unreachable behind an error that points somewhere else. Importing route modules dynamically in dev — the same change the open perf entry "Load route modules on demand in dev" needs — would confine the failure to the route being rendered; short of that, catch the load failure per route entry and let unaffected routes render.

## Invalidate on a `+page`/`+layout` change too: two saves 5-40 ms apart lose the second one forever

`packages/run/src/vite/plugin.ts` › `configureServer` | 2026-08-21 | impact:high | effort:med

The dev watcher's `all` listener only calls `invalidateVirtualFiles()` and re-emits `change` on the module's importers when the changed routable file is a `Handler`/`Middleware` (or `runtimeInclude`); a `Page`/`Layout` change falls through to Vite and `@marko/vite`, and a second write that lands while the first change is still in flight is dropped there. Writing `src/routes/+page.marko` twice a few milliseconds apart therefore serves the first content indefinitely: sweeping gaps of 5/10/15/20/25/30/40 ms, all seven served the older content while the file on disk held the newer one, twice in a row (14/14), still stale at t+90 s, HTTP 200 throughout, nothing in the dev log and nothing on the HMR socket; a 0 ms gap (one coalesced watcher event) and gaps >=60 ms were always fine, and the identical double-write against a `+handler.ts` never wedged at any gap — which is the tell that the handler branch's extra invalidation is what saves it. Only another save recovers it, so format-on-save, `prettier --write`, a codemod, or an editor that writes then renames leaves the developer reading output that is no longer on disk. Extending that `type === "change"` condition to `Page`/`Layout` closed every previously-wedging gap (0/5 wedges after the change, 5/5 before it, same server and same script), so the shape to land is the narrow half of it — re-emit `change` on the template's importers without the full `invalidateVirtualFiles()` sweep, which would otherwise run on every save — plus a dev-server test that writes one route file twice inside 20 ms and asserts the second content is served. Same listener as the open bulk-rewrite wedge entry, but a one-file deterministic trigger with a different observable: that one hangs, this one serves stale.

## Escape the dev error page's `<h2>`/`<pre>` as HTML, not as a JS string; the code frame renders as `\u003c`

`packages/run/src/adapter/dev-server.ts` › `createErrorMiddleware` | 2026-08-21 | impact:med | effort:low

`stripHtml` is `string.replace(/</g, "\\u003c")` — a JavaScript string escape, correct for the JSON embedded in `<script type="module">` where it stops a `</script>` breakout, but `createErrorMiddleware` also runs the message and the stack through it before interpolating them into `<h2>` and `<pre>`, where the browser has no JS to unescape them. Breaking any `.marko` file and fetching the route renders the frame as `1 | \u003csection>`, `2 |   \u003ch1>BROKEN\u003c`, `> 3 | \u003c/section>`, so the one part of the page a human reads is the part that is illegible, while the same frame prints correctly in the terminal. Use HTML entity escaping (`&lt;`, `&amp;`) for the two body interpolations and keep `stripHtml` for the script payload only. While there, the machine-readable copy is a bare `const error = {…}` object literal with no terminating semicolon inside a module script, which is awkward to extract from CI or a scraper; emitting the same payload as `<script type="application/json" id="marko-run-error">` beside it would make the page serve both audiences.

## Honor the app's `server.hmr` config in dev; today `hmr.port` is silently replaced with a random one

`packages/run/src/adapter/dev-server.ts` › `createViteDevServer` | 2026-08-21 | impact:med | effort:low

`createViteDevServer` reads `hmr` off the _inline_ config it is handed and only picks its own port when the app "names no port (nor a server or client port to honor)" — but `dev()` in `packages/run/src/cli/commands.ts` builds that inline config as `{ root, configFile, plugins }` with no `server` key at all, so the check always sees `undefined` and always installs `server.hmr = { port: await getAvailablePort(24678) }`, which as inline config outranks whatever `vite.config.ts` resolved to. With `server: { hmr: { port: 42599 } }` in `vite.config.ts` (config demonstrably loaded, 42599 demonstrably free) the websocket server bound `39327` on one run and `40047` on another; adding `clientPort: 42598` alongside it does reach the browser (`const hmrPort = 42598` in `/@vite/client`) while the server still listens on the random port, so the one combination an app can express is a client pointed at a port nothing serves. That is the case the intent comment is written for — a container or port-forward that exposes fixed ports — and it is also why live reload cannot be pinned from `marko-run dev`. Forward the resolved `server.hmr` (the `resolveConfig` result the CLI already computes for `server.port`) into that inline config, or read it from `devServer.config` and only default when it is genuinely unset.

## Emit a server source map from `marko-run build`; the starter's own `--enable-source-maps` is a no-op

`packages/run/src/vite/plugin.ts` › `config` | 2026-08-21 | impact:med | effort:low

`pluginConfig.build.sourcemap ??= config.build?.sourcemap ?? (isBuild && !isSSRBuild)` turns source maps off for the SSR pass, so `marko-run build` writes `dist/index.mjs` with no `dist/index.mjs.map` while still emitting `dist/public/assets/*.js.map` for the client — the opposite of what a deploy wants, and the reason the `start` script every scaffold ships (`node --enable-source-maps ./dist/index.mjs`) does nothing. A production throw after the response has flushed prints `at Timeout._onTimeout (…/dist/index.mjs:860:37)`; adding `build: { sourcemap: true }` to `vite.config.ts` emits the map and the same throw prints `at Timeout._onTimeout (…/src/probe.ts:32:24)`, so the fix is only the default. That escape hatch is also the wrong lever, because `config.build?.sourcemap` applies to both passes: an app that wants readable server stacks cannot get them without also re-publishing its original template source to the browser, which the open entry "Default the client build's `sourcemap` off (or to `hidden`)" is trying to stop. Default the SSR pass to `true` (or `"hidden"`, since nothing serves `dist/index.mjs.map` to a client) and let the client pass keep its own default, so the two are independently settable.

## Log the error the generated `fetch` swallows; a production 500 leaves no trace anywhere

`packages/run/src/vite/codegen/index.ts` › `renderFetch` | 2026-08-21 | impact:high | effort:low

`renderFetch` emits `catch (error) { if (import.meta.env.DEV) throw error; return new Response(null, { status: 500 }); }`, so in a production build the error object is discarded without ever being read. On the `app` starter with `src/routes/boom/+handler.ts` throwing `intentional-boom-marker-9271`, `PORT=… NODE_ENV=production node --enable-source-maps ./dist/index.mjs > server.log 2>&1` answers `GET /boom` with `500` and `content-length: 0`, and `server.log` is **0 bytes** after two such requests; the identical throw under `marko-run dev` prints `Request failed with error: …` plus a full stack from `createErrorMiddleware` (`packages/run/src/adapter/dev-server.ts`), and a post-flush throw in the same production server _does_ print a stack from `createMiddleware`'s catch — so this one branch is the only silent path, not a policy. Adding a `+500.marko` does not help an operator: an HTML request then renders the error page but still logs nothing, and an `Accept: application/json` request to the same route still returns a bodiless 500 with an empty log, so every API 500 is invisible. Log the error (and the method/URL, which even the post-flush stack lacks) from that catch before returning the 500, or give the generated router an `onError` seam adapters can hook — `console.error` here is the same call the middleware already makes one layer up, and the open entry "Write a 500 status instead of destroying the socket when the middleware has no `next`" assumes a `console.error` that this pre-flush path never reaches.
