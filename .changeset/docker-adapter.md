---
"@marko/run-adapter-docker": minor
---

Add a Docker adapter that builds a self-contained Node.js server and generates a Dockerfile for deploying @marko/run apps as a container (Fly.io, Railway, Render, Cloud Run, ECS, Kubernetes, etc.).
