---
"@marko/run-adapter-aws-lambda": minor
---

Add an AWS Lambda adapter for deploying @marko/run apps to Lambda (Function URLs / API Gateway HTTP API, payload format v2), including static asset serving and a local preview server.
