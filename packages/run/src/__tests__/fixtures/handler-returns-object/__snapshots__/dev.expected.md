# Loading

```html
home
```

# Step 0
ctx=>namesTheHandlerAndTheContract(ctx)

# Errors

```
Request failed with error: Handler 'handler' for GET /api returned a value of type "object" instead of a Response. Return `Response.json(value)` to send JSON, return a `new Response(...)`, or call `next()` to continue.
      at [Normalized Error Stack]
```

